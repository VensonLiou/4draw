@import '../../css/mixins.styl'
@import '../../css/variables.styl'

.container
  Subtitle(500)
  color Warning
  min-width 280px

.total
  margin-bottom 20px
  Subtitle(700)
  flexbox(row, space-between, center, 16px)

.betTypeList
  flexbox(column, flex-start, flex-start, 8px)

.betType
  width 100%
  flexbox(row, space-between, center, 16px)
.row
  flexbox(row, space-between, center)

.dot
  height 6px
  width 6px
  min-width 6px
  background-color currentColor
  border-radius 100px
  margin-inline 16px
  @media ({mobileL}) {
    margin-inline 8px
  }
